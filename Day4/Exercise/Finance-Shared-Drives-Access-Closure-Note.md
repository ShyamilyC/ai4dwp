Resolved. Cause: The drive mapping script was migrated from a USER-context GPO logon script to an Intune PowerShell script running as SYSTEM, so UNC access and user credentials were not available at login time. Action: Restored the Finance drive-mapping behavior with the approved user-context fix. Preventive: Keep user drive mappings in USER context and validate execution context, UNC readiness, and retry behavior before rollout. User confirmed working.